<svg xmlns="http://www.w3.org/2000/svg" width="35" height="35">
    <circle cx="17.5" cy="17.5" r="17.5" fill="#C8EEEE" />
    <text id="lololo" x="50%" y="50%" text-anchor="middle" fill="white" font-size="20px" font-family="Arial" dy=".3em"/>
    <script>
        var url = window.location.href;
        var nmb = url.substring(url.lastIndexOf('=') + 1);
        console.log('LOLOLOOLOL', nmb)
        document.getElementById('lololo').textContent = nmb
    </script>
</svg>